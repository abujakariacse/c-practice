#include <stdio.h>

int main()
{
    // string array (Separate charecter)
    char str[10] = {'a', 'b', 'c', 'd'};

    // string input
    char str2[10] = "abcdefgh";

    printf("%s", str2);
    return 0;
}